/// @description Draw events
// You can write your code in this editor

if (dashing){
	draw_line_width_color(x, y, x-lengthdir_x(128, direction), y-lengthdir_y(128, direction), 4, c_white, c_gray);
}

draw_self();